# Copy to `.env.db.prod` (gitignored) for `npm run db:migrate:prod` / `db:push:prod` / `db:studio:prod`.
# Use your Neon / Vercel Postgres **production** connection string only on trusted machines.
DATABASE_URL=
