# Blueprint: https://render.com/docs/blueprint-spec
# After deploy, add your Render URL in the Clerk Dashboard (Frontend API / redirects)
# and set the Clerk webhook URL to: https://<this-service>.onrender.com/api/webhooks/clerk
services:
  - type: web
    name: flipvise
    runtime: node
    plan: free
    branch: main
    # --include=dev: NODE_ENV=production would otherwise skip tsx/typescript (needed by prebuild/build)
    # unrs-resolver >=1.12 fixes napi-postinstall "Permission denied" on Render
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20
      - key: NPM_CONFIG_PRODUCTION
        value: "false"

      # Clerk Authentication
      - key: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        sync: false
      - key: CLERK_SECRET_KEY
        sync: false
      - key: CLERK_WEBHOOK_SECRET
        sync: false

      # Neon Database
      - key: DATABASE_URL
        sync: false

      # Vercel Blob Storage
      - key: BLOB_READ_WRITE_TOKEN
        sync: false

      # OpenAI
      - key: OPENAI_API_KEY
        sync: false
