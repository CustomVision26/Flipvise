/**
 * @deprecated Import `AdminTabs` from `@/components/admin-tabs` directly.
 * Kept for backwards compatibility — no longer uses client-only dynamic import.
 */
export { AdminTabs as AdminTabsDynamic } from "@/components/admin-tabs";
